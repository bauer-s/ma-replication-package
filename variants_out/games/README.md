# Games in folders

The type of games in the variants folder are written in `*.txt` files in this folder.

After executing the therefore written jupyter notebook it should look like this:
- BoatRace.txt
- FruitCatching.txt
- ...

Depending on the games in the variants folder, the input of the folder can differ.